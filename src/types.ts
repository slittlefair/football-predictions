export interface LeaderboardEntry {
  name: string;
  points: number;
}

export interface TournamentPredictions {
  Winner: string;
  RunnerUp: string;
  ThirdPlace: string;
  FourthPlace: string;
  TopScorer: string;
}
